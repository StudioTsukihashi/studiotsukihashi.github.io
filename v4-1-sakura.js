(()=>{const layer=document.querySelector("[data-sakura-layer]");if(!layer)return;if(window.matchMedia("(prefers-reduced-motion: reduce)").matches)return;const random=(min,max)=>Math.random()*(max-min)+min;const count=Math.min(34,Math.max(18,Math.round(window.innerWidth/52)));const fragment=document.createDocumentFragment();for(let i=0;i<count;i+=1){const petal=document.createElement("span");petal.className="sakura-petal";petal.style.setProperty("--start-x",`${random(-4,102)}vw`);petal.style.setProperty("--petal-size",`${random(7,15)}px`);petal.style.setProperty("--petal-opacity",random(.32,.78).toFixed(2));petal.style.setProperty("--fall-duration",`${random(14,25)}s`);petal.style.setProperty("--wind-duration",`${random(3,7)}s`);petal.style.setProperty("--delay",`${random(-25,0)}s`);petal.style.setProperty("--drift-one",`${random(-5,7)}vw`);petal.style.setProperty("--drift-two",`${random(-10,11)}vw`);petal.style.setProperty("--drift-three",`${random(-14,15)}vw`);fragment.appendChild(petal)}layer.appendChild(fragment)})();
